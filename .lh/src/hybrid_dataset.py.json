{
    "sourceFile": "src/hybrid_dataset.py",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 0,
            "patches": [
                {
                    "date": 1772972186594,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                }
            ],
            "date": 1772972186594,
            "name": "Commit-0",
            "content": "import torch\r\nimport cv2\r\nimport numpy as np\r\nfrom torch.utils.data import Dataset\r\nfrom pathlib import Path\r\nfrom torchvision import transforms\r\n\r\n\r\ndef extract_fft(image_rgb, size=224):\r\n    \"\"\"Extract FFT magnitude map dari gambar RGB\"\"\"\r\n    gray = cv2.cvtColor(image_rgb, cv2.COLOR_RGB2GRAY)\r\n    gray = cv2.resize(gray, (size, size))\r\n    \r\n    fft = np.fft.fft2(gray)\r\n    fft_shift = np.fft.fftshift(fft)\r\n    magnitude = np.log(np.abs(fft_shift) + 1)\r\n    \r\n    # Normalize\r\n    magnitude = (magnitude - magnitude.min()) / (magnitude.max() - magnitude.min() + 1e-8)\r\n    \r\n    return magnitude.astype(np.float32)  # (H, W)\r\n\r\n\r\nclass HybridDataset(Dataset):\r\n    def __init__(self, data_dir, split='train', spatial_transform=None):\r\n        self.data_dir = Path(data_dir) / split\r\n        self.spatial_transform = spatial_transform\r\n        \r\n        self.image_paths = []\r\n        self.labels = []\r\n        \r\n        for img_path in (self.data_dir / 'real').glob('*.jpg'):\r\n            self.image_paths.append(img_path)\r\n            self.labels.append(0)\r\n        \r\n        for img_path in (self.data_dir / 'fake').glob('*.jpg'):\r\n            self.image_paths.append(img_path)\r\n            self.labels.append(1)\r\n        \r\n        print(f\"[{split}] Real: {self.labels.count(0)} | Fake: {self.labels.count(1)}\")\r\n    \r\n    def __len__(self):\r\n        return len(self.image_paths)\r\n    \r\n    def __getitem__(self, idx):\r\n        img = cv2.imread(str(self.image_paths[idx]))\r\n        img_rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)\r\n        \r\n        # Spatial input\r\n        if self.spatial_transform:\r\n            spatial = self.spatial_transform(img_rgb)\r\n        \r\n        # Frequency input\r\n        fft_map = extract_fft(img_rgb, size=224)\r\n        freq = torch.tensor(fft_map).unsqueeze(0)  # (1, 224, 224)\r\n        \r\n        label = torch.tensor(self.labels[idx], dtype=torch.long)\r\n        return spatial, freq, label\r\n\r\n\r\ndef get_spatial_transform(split='train'):\r\n    from torchvision import transforms\r\n    if split == 'train':\r\n        return transforms.Compose([\r\n            transforms.ToPILImage(),\r\n            transforms.Resize((224, 224)),\r\n            transforms.RandomHorizontalFlip(p=0.5),\r\n            transforms.RandomRotation(degrees=10),\r\n            transforms.ColorJitter(brightness=0.2, contrast=0.2),\r\n            transforms.ToTensor(),\r\n            transforms.Normalize(mean=[0.485, 0.456, 0.406],\r\n                                 std=[0.229, 0.224, 0.225])\r\n        ])\r\n    else:\r\n        return transforms.Compose([\r\n            transforms.ToPILImage(),\r\n            transforms.Resize((224, 224)),\r\n            transforms.ToTensor(),\r\n            transforms.Normalize(mean=[0.485, 0.456, 0.406],\r\n                                 std=[0.229, 0.224, 0.225])\r\n        ])"
        }
    ]
}